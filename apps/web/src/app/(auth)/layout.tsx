/**
 * Layout for (auth) route group — login + onboarding pages.
 * No navigation bar; minimal chrome.
 */
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
